/* ============================================================
   Firebase configuration  ── THIS IS THE ONLY FILE YOU EDIT ──

   To turn on REAL accounts that work across devices:
   1. Follow SETUP-FIREBASE.md (takes ~5 minutes).
   2. Replace the placeholder values below with your project's config.

   Until you do this, the app runs in LOCAL mode (accounts saved only
   on this device) — so it keeps working out of the box.
   ============================================================ */
window.FIREBASE_CONFIG = {
  apiKey: "YOUR_API_KEY",
  authDomain: "YOUR_PROJECT.firebaseapp.com",
  projectId: "YOUR_PROJECT_ID",
  storageBucket: "YOUR_PROJECT.appspot.com",
  messagingSenderId: "YOUR_SENDER_ID",
  appId: "YOUR_APP_ID",
};
